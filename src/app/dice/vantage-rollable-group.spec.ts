import { VantageRollableGroup } from './vantage-rollable-group';

describe('VantageRollableGroup', () => {
  it('should create an instance', () => {
    expect(new VantageRollableGroup()).toBeTruthy();
  });
});
