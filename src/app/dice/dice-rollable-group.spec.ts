import { DiceRollableGroup } from './dice-rollable-group';

describe('DiceRollableGroup', () => {
  it('should create an instance', () => {
    expect(new DiceRollableGroup()).toBeTruthy();
  });
});
